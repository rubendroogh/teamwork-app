// Add your native dependencies here:

android {
  defaultConfig {  
    generatedDensities = []
    applicationId = "org.daboiz.teamwork"
  }
  aaptOptions {  
    additionalParameters "--no-version-vectors"  
  }  
} 
